import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  standalone: true,
  selector: 'app-country-results',
  templateUrl: './country-results.component.html', 
  imports: [RouterModule]
})
export class CountryResultsComponent {  
}